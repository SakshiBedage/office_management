// import React, { createContext, useState, useEffect } from "react";

// const WorkspaceContext = createContext();

// const WorkspaceProvider = ({ children }) => {
//   // Retrieve workspace data from local storage on component mount
//   const [workspace, setWorkspace]

// //   = useState(() => {
// //     const storedWorkspace = localStorage.getItem("workspace");
// //     return storedWorkspace ? storedWorkspace : "";
// //   });
// //   console.log("workspacr is called", workspace);
// //   // Update local storage when workspace changes
// //   useEffect(() => {
// //     localStorage.setItem("workspace", workspace);
// //   }, [workspace]);
//   console.log("workspacr is called ghbtgrhtrg", setWorkspace);
//   return (
//     <WorkspaceContext.Provider value={{ workspace, setWorkspace }}>
//       {children}
//     </WorkspaceContext.Provider>
//   );
// };

// export { WorkspaceProvider, WorkspaceContext };
